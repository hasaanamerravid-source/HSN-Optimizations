[package]
name = "hsn_lod"
version = "0.1.0"
edition = "2021"
description = "Zero-alloc LOD distance thresholds over packed XYZ."
license = "MIT"

[lib]
crate-type = ["cdylib", "staticlib"]

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
panic = "abort"
strip = true
