# Prebuilt Linux kernels

Optional objects for local `-PbundleNatives=true` builds.

Shipped here: `libhsn_c.so`, `libhsn_cpp.so`, `libhsn_asm.so`,
`libhsn_horizon.so`, `libhsn_hotpath.so`, `libhsn_lod.so`,
`libhsn_pipeline.so`.

Default `./gradlew build` still produces a Java-only jar.
